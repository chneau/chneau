version: '3.7'
services:
  chneau:
    image: chneau
    networks:
      - app
    deploy:
      mode: replicated
      replicas: 4
      restart_policy:
        condition: any
      update_config:
        order: start-first
        delay: 1s
        parallelism: 1
    healthcheck:
      test: wget --quiet --tries=1 --spider http://localhost:$$PORT/health || exit 1
      interval: 15s
      timeout: 3s
      retries: 4
      start_period: 5s
    environment:
      PORT: ${PORT:-8000}
networks:
  app:
