workspace:
  base: /go
  path: src/github.com/chneau/chneau

pipeline:
  build:
    image: golang
    commands:
      - go get -v
      - go build
  check:
    image: alpine
    commands:
      - ls -la